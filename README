```sh
# start postgres
docker run --rm --name postgres -e POSTGRES_PASSWORD=password -p 127.0.0.1:5432:5432 -d postgres

# install deps
poetry install

# run python script
poetry run python demo/main.py

# run psql
PGPASSWORD=password psql -h 127.0.0.1 -p 5432 -U postgres

# check pg activity
SELECT state, query FROM pg_stat_activity WHERE datname = 'postgres';
```
